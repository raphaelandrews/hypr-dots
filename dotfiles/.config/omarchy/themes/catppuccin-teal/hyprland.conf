# Hyprland Colors - Variant 1 (Teal 185°)
$activeBorderColor = rgb(8bd5ca)
$inactiveBorderColor = rgb(415c61)

general {
    # Using a solid color for the border
    col.active_border = $activeBorderColor
    col.inactive_border = $inactiveBorderColor

    # Optional: Gradient matching the new Teal-Sky flow:
    # col.active_border = rgba(8bd5caee) rgba(91d7e3ee) 45deg
}

group {
    col.border_active = $activeBorderColor
    col.border_inactive = $inactiveBorderColor

    groupbar {
        col.active = $activeBorderColor
        col.inactive = $inactiveBorderColor
        text_color = rgb(c0dce0)
    }
}