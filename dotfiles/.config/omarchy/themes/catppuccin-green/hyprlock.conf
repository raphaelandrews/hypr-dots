# Hyprlock Colors - Variant 3 (Green-Teal 165°)

# Main Background (Crust)
$color = rgba(17, 34, 27, 1.0)

# Input Field Background (Base)
$inner_color = rgba(30, 54, 44, 0.8)

# Active/Primary Accents (Green)
$outer_color = rgba(166, 218, 149, 1.0)
$font_color = rgba(166, 218, 149, 1.0)
$outer_aura = rgba(166, 218, 149, 0.55)

# Status Accents (Yellow & Maroon)
$check_color = rgba(238, 212, 159, 1.0)
$fail_color = rgba(238, 153, 160, 1.0)

# input-field {
#     monitor =
#     size = 650, 100
#     position = 0, 0
#     halign = center
#     valign = center

#     inner_color = $inner_color
#     outer_color = $outer_color
#     outer_color_inactive = $outer_aura
#     outline_thickness = 4
    
#     font_family = CaskaydiaMono Nerd Font
#     font_color = $font_color

#     placeholder_text = Enter Password 🔑
#     check_color = $check_color
#     fail_color = $fail_color
#     fail_text = <i>$FAIL ($ATTEMPTS)</i>

#     rounding = 4
#     shadow_passes = 0
#     fade_on_empty = false
# }