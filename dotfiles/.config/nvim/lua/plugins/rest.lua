-- REST client (kulala.nvim) via the official LazyVim extra.
-- Provides .http file support, <leader>R* keymaps and treesitter http/graphql.
return {
  { import = "lazyvim.plugins.extras.util.rest" },
}
